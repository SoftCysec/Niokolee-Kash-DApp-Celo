import React from 'react';
import BorrowPage from '../components/BorrowPage.js';

const Borrow = () => {
  return (
    <div>
      <h1>Borrow</h1>
      <BorrowPage />
    </div>
  );
};

export default Borrow;
